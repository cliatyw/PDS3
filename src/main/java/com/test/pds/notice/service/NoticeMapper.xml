<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.test.pds.notice.service.NoticeMapper">
	<insert id="insertNotice" parameterType="com.test.pds.notice.service.Notice"> 
			<selectKey resultType="int" keyProperty="noticeId" order="AFTER">
				SELECT LAST_INSERT_ID()
			</selectKey>
		INSERT INTO notice(
		notice_title,
		notice_content) 
		VALUES (
		#{noticeTitle},
		#{noticeContent}) 
	</insert> 
	
<!-- 	<insert id="addNoticeFile" 
				parameterType="com.test.pds.notice.service.Notice">
		INSERT INTO notice_file(
			notic_file_name,
			notic_file_ext,
			notic_file_type,
			notic_file_size,
			notice_id
			)VALUES(
			#{noticeFileName},
			#{noticeFileExt},
			#{noticeFileType},
			#{noticeFileSize},
			#{noticeId})
	</insert> -->
</mapper>